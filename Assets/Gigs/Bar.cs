using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bar : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //CalendarEvent e = Calendar.GetCurrentEvent();
        //if (e is GigEvent)
        //{

        //} else if (e is BandPracticeEvent)
        //{

        //} else if (e is JobEvent)
        //{
        //}
        //else if (e is QuestEvent)
        //{
        //    if (((QuestEvent)e).Location().Equals(SceneChanger.Instance.GetCurrentScene()))
        //    {

        //    }
        //}
    }

    private void SetUpBoringState()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
